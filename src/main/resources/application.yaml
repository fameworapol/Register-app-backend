spring:
  jpa:
    hibernate:
      ddl-auto: update
  servlet:
    multipart:
      enabled: true
      max-file-size: 2MB
      max-request-size: 5MB

  datasource:
    url : jdbc:postgresql://localhost:5432/Register
    username : postgres
    password : syspass
